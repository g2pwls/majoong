// src/components/farm/panels/DonationPanel.tsx
export default function DonationPanel({ farmId }: { farmId: string }) {
  return <section id="panel-donations">기부금 사용 내역 패널 (farmId: {farmId})</section>;
}
